import { RelativeStrengthClient } from "@/components/rs/RelativeStrengthClient";

export const metadata = {
  title: "Relative Strength — STOCKER",
  description: "Leaderboard of names leading the broad benchmark on relative strength.",
};

export default function RelativeStrengthPage() {
  return <RelativeStrengthClient />;
}
